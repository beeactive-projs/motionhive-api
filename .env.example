# ===============================================
# MotionHive API - Environment Variables
# ===============================================
# IMPORTANT: Copy this file to .env and fill in your values
# Never commit .env to version control!

# -----------------------------------------------
# Application Configuration
# -----------------------------------------------
NODE_ENV=development
PORT=3000

# -----------------------------------------------
# Database Configuration (PostgreSQL)
# -----------------------------------------------
# Option 1: Connection string (preferred for Neon/production)
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
#
# Option 2: Individual vars (for local development)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=beeact

# -----------------------------------------------
# Redis Configuration (for Bull queues)
# -----------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379

# -----------------------------------------------
# JWT Configuration
# -----------------------------------------------
# CRITICAL: Generate strong secrets! Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=please_generate_a_64_character_random_string_here
JWT_EXPIRES_IN=2h
JWT_REFRESH_SECRET=please_generate_another_different_64_character_random_string
JWT_REFRESH_EXPIRES_IN=7d

# -----------------------------------------------
# Security Configuration
# -----------------------------------------------
# Bcrypt rounds (10-15, higher = more secure but slower)
BCRYPT_ROUNDS=12

# -----------------------------------------------
# Frontend URL (for CORS and email links)
# -----------------------------------------------
# Uncomment and set in production:
# FRONTEND_URL=https://your-frontend-url.vercel.app

# -----------------------------------------------
# Email Configuration (Resend)
# -----------------------------------------------
# Get your API key from https://resend.com/api-keys
# Verify your domain at https://resend.com/domains
RESEND_API_KEY=re_xxxxx

# Sender configuration (optional, defaults shown)
# EMAIL_FROM=noreply@motionhive.fit
# EMAIL_FROM_NAME=MotionHive

# -----------------------------------------------
# OAuth (optional – for Sign in with Google / Facebook)
# -----------------------------------------------
# Google: Create OAuth client at https://console.cloud.google.com/apis/credentials
# GOOGLE_CLIENT_ID=xxxxx.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=GOCSPX-xxxxx

# Facebook: Create app at https://developers.facebook.com/ → Settings → Basic
# FACEBOOK_APP_ID=1234567890123456
# FACEBOOK_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------
# Development Origins (for CORS in production)
# -----------------------------------------------
# Allow localhost origins in production for testing
# Comma-separated list of origins
# ONLY use this for development/testing, REMOVE in final production!
# DEV_ORIGINS=http://localhost:4200,http://localhost:3000

# -----------------------------------------------
# Railway Production URL
# -----------------------------------------------
# motionhive-api-production.up.railway.app